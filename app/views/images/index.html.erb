<p style="color: green"><%= notice %></p>

<% content_for :title, "Images" %>

<h1>Images</h1>

<%= link_to "New image", new_image_path %>

<div id="images">
  <% @images.each do |image| %>
    <%= render partial: "thumb", locals: { image: image } %>

  <% end %>
</div>
